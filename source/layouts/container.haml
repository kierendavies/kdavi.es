= wrap_layout :layout do
  .container
    .page-header
      %h1
        = current_resource.data.title
        - if current_resource.data.subtitle
          %small= current_resource.data.subtitle
    = yield
